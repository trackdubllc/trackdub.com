name: Trigger Kiro Crew

on:
  push:
    branches: [main]
  pull_request:
    types: [opened]
  workflow_dispatch:

jobs:
  trigger:
    uses: tonythethompson/.github/.github/workflows/kiro-crew-trigger.yml@main
    with:
      daytona_os: linux
    secrets: inherit
